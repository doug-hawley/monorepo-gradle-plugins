name: Release

on:
  push:
    tags:
      - 'v*.*.*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version to release (e.g., 1.0.0)'
        required: true

jobs:
  release:
    name: Create Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v3

      - name: Extract version from tag
        id: get_version
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            VERSION="${{ github.event.inputs.version }}"
          else
            VERSION=${GITHUB_REF#refs/tags/v}
          fi
          echo "VERSION=$VERSION" >> $GITHUB_OUTPUT
          echo "Releasing version: $VERSION"

      - name: Build plugin
        run: ./gradlew build --no-daemon --stacktrace

      - name: Run all tests
        run: ./gradlew check --no-daemon --stacktrace

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ steps.get_version.outputs.VERSION }}
          name: Release ${{ steps.get_version.outputs.VERSION }}
          draft: false
          prerelease: false
          files: |
            build/libs/*.jar
            CHANGELOG.md
          body: |
            ## Changes in version ${{ steps.get_version.outputs.VERSION }}
            
            See [CHANGELOG.md](CHANGELOG.md) for full details.
            
            ### Installation
            
            ```kotlin
            plugins {
                id("com.bitmoxie.monorepo-changed-projects") version "${{ steps.get_version.outputs.VERSION }}"
            }
            ```
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # Uncomment this job when you're ready to publish to Gradle Plugin Portal
  # You'll need to add GRADLE_PUBLISH_KEY and GRADLE_PUBLISH_SECRET to your repository secrets
  # publish-plugin:
  #   name: Publish to Gradle Plugin Portal
  #   runs-on: ubuntu-latest
  #   needs: release
  #
  #   steps:
  #     - name: Checkout code
  #       uses: actions/checkout@v6
  #
  #     - name: Set up JDK 17
  #       uses: actions/setup-java@v5
  #       with:
  #         java-version: '17'
  #         distribution: 'temurin'
  #
  #     - name: Setup Gradle
  #       uses: gradle/actions/setup-gradle@v3
  #
  #     - name: Publish to Plugin Portal
  #       env:
  #         GRADLE_PUBLISH_KEY: ${{ secrets.GRADLE_PUBLISH_KEY }}
  #         GRADLE_PUBLISH_SECRET: ${{ secrets.GRADLE_PUBLISH_SECRET }}
  #       run: ./gradlew publishPlugins --no-daemon --stacktrace

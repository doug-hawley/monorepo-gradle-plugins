name: CI

on:
  push:
    branches: [ main, develop, "release-please**" ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

jobs:
  build:
    name: Build and Test
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
        java-version: [17, 21]
      fail-fast: false

    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0  # Full history for git-based tests

      - name: Set up JDK ${{ matrix.java-version }}
        uses: actions/setup-java@v5
        with:
          java-version: ${{ matrix.java-version }}
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v5
        with:
          cache-disabled: true

      - name: Configure Git
        run: |
          git config --global user.email "ci@github.com"
          git config --global user.name "GitHub CI"

      - name: Grant execute permission for gradlew (Unix)
        if: runner.os != 'Windows'
        run: chmod +x gradlew

      - name: Build plugin
        run: ./gradlew :monorepo-build-plugin:build -x test -x unitTest -x functionalTest --no-daemon --stacktrace

      - name: Run unit tests
        run: ./gradlew :monorepo-build-plugin:unitTest --no-daemon --stacktrace

      - name: Run functional tests
        run: ./gradlew :monorepo-build-plugin:functionalTest --no-daemon --stacktrace

      - name: Upload test reports
        if: always()
        uses: actions/upload-artifact@v6
        with:
          name: test-reports-${{ matrix.os }}-java${{ matrix.java-version }}
          path: |
            monorepo-build-plugin/build/reports/tests/
            monorepo-build-plugin/build/test-results/
          retention-days: 7

      - name: Upload build artifacts
        if: matrix.os == 'ubuntu-latest' && matrix.java-version == '17'
        uses: actions/upload-artifact@v6
        with:
          name: plugin-jar
          path: monorepo-build-plugin/build/libs/*.jar
          retention-days: 30

  code-quality:
    name: Code Quality
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v5

      - name: Check code style
        run: ./gradlew :monorepo-build-plugin:check --no-daemon --stacktrace

      - name: Verify plugin descriptor
        run: ./gradlew :monorepo-build-plugin:validatePlugins --no-daemon --stacktrace

  publish-local:
    name: Test Local Publishing
    runs-on: ubuntu-latest
    needs: [build, code-quality]

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Set up JDK 17
        uses: actions/setup-java@v5
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Gradle
        uses: gradle/actions/setup-gradle@v5

      - name: Publish to Maven Local
        run: ./gradlew :monorepo-build-plugin:publishToMavenLocal --no-daemon --stacktrace

      - name: Verify published artifact
        run: |
          if [ -d ~/.m2/repository/io/github/doug-hawley/monorepo-build-plugin ]; then
            echo "✅ Plugin published successfully to Maven Local"
            ls -la ~/.m2/repository/io/github/doug-hawley/monorepo-build-plugin/
          else
            echo "❌ Plugin not found in Maven Local"
            exit 1
          fi
